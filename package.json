{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Hexlet Chat is a simplified analog of Slack chat. Real-time application written in React/Redux using AJAX, REST, websockets, React (with hooks) + Redux (@reduxjs/toolkit) + Formik + React Bootstrap.",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd ./frontend && npm ci"
  },
  "author": "AxeRicin",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.9"
  },
  "engines": {
    "node": "18.17.1"
  }
}
